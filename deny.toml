# MCP-RE supply-chain policy — cargo-deny (audit gap G-4, MCPS-077, issue #3980)
#
# This is the authoritative supply-chain policy for the firewalled MCP-RE module.
# It is enforced by the CI gate `.github/workflows/mcp-re-supply-chain.yml`
# (`cargo deny check {licenses,bans,sources,advisories}`), NOT by Bazel:
# cargo-deny needs network (advisory-DB fetch) and a prebuilt binary, so it is a
# CI policy gate by design, not a hermetic Bazel test (per the G-4 remediation
# decision — Option 1). It is deliberately NOT added to any Cargo.toml and NOT
# pulled into the `crates_mcp_re` crate universe.
#
# The policy travels WITH the module: this repository is self-contained
# (ADR-MCPS-001/012) and intended for future standalone extraction to a public
# repository, where this same `deny.toml` runs unchanged from the repo root.
#
# This one policy governs ALL FOUR Cargo lockfiles in the repository — the root
# workspace, both SDK crate universes, and the test-only mock PKCS#11 provider.
# A crate is only assessed if the manifest that reaches it is checked, so the CI
# gate runs every one of them.
#
# Validate locally (cargo-deny pinned to the SAME version as the CI workflow —
# `CARGO_DENY_VERSION` in that file):
#   cargo deny --manifest-path Cargo.toml check advisories licenses bans sources
#   cargo deny --manifest-path sdk/python/Cargo.toml check advisories licenses bans sources
#   cargo deny --manifest-path sdk/typescript/Cargo.toml check advisories licenses bans sources
#   cargo deny --manifest-path mcp-re-proxy/tests/mock-pkcs11/Cargo.toml check advisories licenses bans sources

# ---------------------------------------------------------------------------
# [graph] — the dependency surface that every check below is evaluated against.
# ---------------------------------------------------------------------------
[graph]
# Restrict evaluation to the platforms MCP-RE actually ships/runs on. This PRUNES
# target-conditional crates that are pulled in only for other targets (in this
# tree: the `wasi` crate and the whole `windows-*` family, and transitively any
# wasm32 wit-bindgen/wasm-encoder crates) so they are not assessed for licenses
# or advisories. Verified: with this filter `cargo deny ... list` shows no
# wasi/windows/wit-bindgen crates; without it, they all appear.
targets = [
    "x86_64-unknown-linux-gnu",
    "aarch64-apple-darwin",
    "x86_64-apple-darwin",
]

# ---------------------------------------------------------------------------
# [advisories] — RustSec advisory DB enforcement (vulns / unmaintained /
# unsound / yanked). DB is fetched from GitHub at check time (network needed —
# fine in CI and locally). Uses the current schema (`version = 2`); the old
# `vulnerability`/`unmaintained`/`unsound` severity keys are deprecated and
# removed — under v2 every advisory class is denied unless explicitly listed in
# `ignore`, and yanked crates are controlled by `yanked` below.
# ACCEPTANCE: this gate FAILS on any RUSTSEC vulnerability, unmaintained/unsound
# advisory, or yanked crate. We do NOT pre-populate `ignore` — genuine findings
# must surface and be triaged, never silently suppressed.
# ---------------------------------------------------------------------------
[advisories]
version = 2
# Treat a crate version pulled from the registry that was later yanked as a hard
# failure (a yanked dep is a supply-chain integrity signal).
yanked = "deny"
# ignore = []  # intentionally empty — do not suppress real advisories here.

# ---------------------------------------------------------------------------
# [licenses] — explicit allow-list, derived from the ACTUAL resolved tree under
# the targets above (discovered via `cargo deny ... list`). The workspace itself
# is Apache-2.0. No "allow everything", no copyleft slipping through.
# ---------------------------------------------------------------------------
[licenses]
version = 2
# Require reasonably high textual confidence when matching a LICENSE file to an
# SPDX id (cargo-deny default is 0.8); below this it is reported, not guessed.
confidence-threshold = 0.8
allow = [
    "Apache-2.0",   # workspace license + most of the RustCrypto/rustls tree
    "MIT",          # dual-licensed crates (serde, libc, syn, ...) + memchr's "Unlicense OR MIT"
    "BSD-3-Clause", # curve25519-dalek, ed25519-dalek, subtle, redis
    "ISC",          # rustls, rustls-webpki, untrusted, and part of ring
    "Unicode-3.0",  # unicode-ident
    "Unlicense",    # memchr (offered as "Unlicense OR MIT")
    # webpki-roots (0.26 shim + 1.0), reached via ureq's `tls` feature. CDLA-
    # Permissive-2.0 is a DATA license: the crate ships the Mozilla CA root set,
    # and the CDLA governs that data, not code linked into MCP-RE. It is
    # permissive with no reciprocal/share-alike obligation on downstream output.
    # This is the crate's ONLY license (no dual-license alternative to fall back
    # on), so the id has to be allowed for the gate to see the tree at all.
    "CDLA-Permissive-2.0",
    # target-lexicon, a BUILD dependency of pyo3 in sdk/python. The LLVM
    # exception only RELAXES Apache-2.0 (it removes the object-code attribution
    # requirement), so allowing it grants nothing beyond plain Apache-2.0, which
    # is already allowed above. Listed as the full expression rather than
    # widening the policy with `exceptions`.
    # Required by `target-lexicon` (via pyo3) in the sdk/python graph ONLY. The other
    # three manifests therefore report `license-not-encountered` for it — that warning is
    # inherent to one policy file covering four different dependency graphs, and is not a
    # sign of dead configuration. Removing this line to silence the warning makes
    # `cargo deny --manifest-path sdk/python/Cargo.toml check licenses` FAIL outright.
    "Apache-2.0 WITH LLVM-exception",
    # xxhash-rust, reached via redis 1.x (it hashes cluster slot keys). BSL-1.0 is
    # OSI-approved and FSF Free/Libre, permissive, with no reciprocal/share-alike
    # obligation and no binary-attribution requirement at all — strictly weaker
    # obligations than the MIT and Apache-2.0 already allowed above. It is the
    # crate's ONLY license (no dual-license alternative), so the id has to be
    # allowed for the gate to see the tree.
    "BSL-1.0",
]
# `ring` is the well-known non-trivial case: its SPDX expression resolves to
# "Apache-2.0 AND ISC" (Apache-2.0 for ring's own code, ISC/BoringSSL for the
# vendored crypto). Both ids are in `allow` above, so ring passes HONESTLY with
# no blanket allow-all and no per-crate license override / clarify needed —
# verified: `check licenses` reports `licenses ok` with ring present.

# ---------------------------------------------------------------------------
# [bans] — duplicate-version and wildcard-dependency hygiene.
# ---------------------------------------------------------------------------
[bans]
# Duplicates stay at "warn" IN GENERAL: a transitive bump that pulls a second
# major of e.g. getrandom or thiserror should surface a warning, not block the
# gate on a non-security issue. The tree carries several such duplicates —
# webpki-roots 0.26.11 (a thin re-export shim) alongside 1.0.7 from ureq;
# asn1-rs 0.6/0.7 from the two x509-parser generations; thiserror 1/2.
multiple-versions = "warn"

# ...but NOT for the crates that decide how MCP-RE hashes. `sha2` is the
# implementation the canonical preimage runs through and `block-buffer` is its
# internal buffer. Two versions of those in one graph does not mean "an old copy
# lingers" — it means the signing binary links TWO independent SHA-256
# implementations, and which one executes depends on which crate called it: our
# preimage hashing with one while ed25519-dalek hashes internally with the other.
# For a product whose entire claim is a reproducible signed preimage, that is a
# split-brain in the one primitive the claim rests on. It must fail the gate
# rather than scroll past in a warning list — which is exactly how it got in:
# sha2 0.11 landed via an unattended Dependabot bump (PR #457) and forked all
# four lockfiles with nothing going red.
#
# `digest` and `crypto-common` are the TRAIT crates of that generation and are
# listed too, but with one documented exception below: `x509-ocsp` 0.2.1 (its
# `builder` feature => digest + signature) has no digest-0.11 release, and it is
# the last crate in the tree holding 0.10. The exception is scoped to its subtree
# via `skip-tree`, NOT granted globally, because x509-ocsp hashes nothing we sign
# — it uses those traits for OCSP CertID/DER machinery, and our own call site
# deliberately avoids handing it our `Sha256` (see the hand-built CertID in
# mcp-re-proxy/src/ocsp.rs). Delete the skip-tree the day x509-ocsp ships on
# digest 0.11; `sha2` and `block-buffer` stay unconditionally single-version.
#
# The `[[bans.deny]]` array that carries this rule is at the END of this section:
# every bare `[bans]` key has to precede it, or TOML binds the key to the last
# array entry instead of to `[bans]`.
#
# Allow `path = "../x"` intra-workspace dependencies (which Cargo records as a
# `*` version requirement). These are local path links, not registry wildcards,
# so they carry no supply-chain risk. `allow-wildcard-paths` does NOT exempt them
# here because the workspace member crates are not marked `publish = false`
# (and this is a policy/config-only change that does not edit crate manifests),
# so a real `*` registry dependency and these benign path wildcards are both
# surfaced at "warn": loud enough to notice a genuine registry `*`, without
# failing the gate on intra-workspace path deps. Tighten to "deny" if the member
# crates ever gain `publish = false`.
wildcards = "warn"
allow-wildcard-paths = true

# The RustCrypto hashing core, one generation only — rationale above.
[[bans.deny]]
name = "digest"
deny-multiple-versions = true

[[bans.deny]]
name = "block-buffer"
deny-multiple-versions = true

[[bans.deny]]
name = "crypto-common"
deny-multiple-versions = true

[[bans.deny]]
name = "sha2"
deny-multiple-versions = true

# `rand_core` cannot be unified today, so it is constrained rather than left loose.
# The 0.6.4 instance sits inside the `x509-ocsp` skip-tree below and so needs no skip of
# its own — verified: adding one changed nothing. This rule therefore catches a
# rand_core appearing anywhere OUTSIDE that subtree, which is the case worth failing on.
#
# Both sides are forced by upstream and neither is ours to move:
#
#   0.10.1  p256["ecdsa"] -> ecdsa/algorithm -> HARD dep:rfc6979 -> crypto-bigint
#           /rand_core. `rand_core` is optional in crypto-bigint, but ecdsa 0.17 has
#           no verify-only feature — `algorithm` is the umbrella and enables rfc6979
#           unconditionally — so no feature combination reaches P-256 verification
#           without it.
#   0.6.4   x509-ocsp 0.2.1 -> signature 2.2.0. 0.2.1 is the latest release; there is
#           nothing to bump to.
#
# Unifying would mean giving up either ES256 receipt verification (MCPRE-501) or OCSP.
# `rand_core` is a trait crate, so two copies mean two trait definitions rather than
# two RNG implementations, and no MCP-RE signing path uses either: Ed25519 signing goes
# through ed25519-dalek, and the P-256 code verifies only.
#
# Revisit when x509-ocsp publishes against signature 3, or ecdsa exposes verification
# without rfc6979 — at which point this whole block should be deleted, not widened.
[[bans.deny]]
name = "rand_core"
deny-multiple-versions = true

# The single documented holdout — rationale above. `x509-ocsp` 0.2.1 is the latest
# release and still pulls signature 2 / digest 0.10 / crypto-common 0.1 through its
# `builder` feature. Scoped to that subtree so the duplicate cannot appear anywhere
# else in the graph unnoticed. Remove when x509-ocsp publishes on digest 0.11.
# depth 5 reaches crypto-common, the deepest node of the held-back generation
# (x509-ocsp -> signature -> digest -> crypto-common). Kept bounded rather than
# unlimited so the exception cannot silently swallow unrelated duplicates.
[[bans.skip-tree]]
name = "x509-ocsp"
version = "=0.2.1"
depth = 5

# ---------------------------------------------------------------------------
# [sources] — only crates.io is a trusted origin. Any crate from an unknown
# registry or any git source is a hard failure (no vendored git deps allowed).
# ---------------------------------------------------------------------------
[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
