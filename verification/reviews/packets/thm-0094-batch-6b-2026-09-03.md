<!-- SPDX-License-Identifier: Apache-2.0 -->
# Owner review packet 6B — THM-0094 after the bounded-reader repair

**One subject.** ADR-MCPRE-059 §14.7 / §28, issue #746. Layer 1: evidence about the tree,
not an approval and not authoritative state. THM-0095 and THM-0042 are deliberately absent.

Supersedes `thm-0094-python-sdk-root-2026-09-03.md`, which was held under rulings A–F. That
packet stays as it was measured; this one carries the repaired tree.

---

## 1. The claim as it now stands

### Title — unchanged

The shipped Python SDK accepts only an answer to its own request

### Statement

> For the shipped Python SDK, an application is not handed, as this call's answer, a response
> from another exchange or signer, or one that verified only unbound — and is not led to
> repeat a side effect by reading silence as *it did not run*.
>
> Every reply is taken from the correlation entry the request created, and a reply binding to
> nothing outstanding, arriving late, or repeating one already answered is refused rather than
> delivered. A response is accepted only under the configured trust anchor, epoch, audience and
> revocation posture, measured against a RECORDED delegated session rather than a constructed
> one. A notification is delivered only against a signed acknowledgement. An elicitation pauses
> a call rather than ending it, so a reply that is not terminal — including one whose result
> type this SDK does not recognise — is refused rather than surfacing as the call's answer.
> A verified reply that is not an answer to this request — one carrying a method, or one with
> neither result nor error — is refused rather than dispatched.
>
> What the application is told about execution is what the receipt said, and nothing else. A
> verified receipt carrying execution and retry fields is reported with exactly those fields;
> a member the receipt did not carry is not invented for it. A LOCAL failure — a transport
> deadline, a cancellation, a local I/O or signing failure — is reported as a local and
> ambiguous failure under the SDK's own prefix: never as `not_executed`, never as retry-safe,
> and never as a wire code the peer did not send.

The final paragraph is the amendment. Its shape is the ruled one:

```text
verified receipt with execution/retry fields → report exactly those fields
verified receipt without a member           → do not invent that member
local timeout / cancellation / I/O failure  → local, ambiguous
                                            → NOT not_executed
                                            → NOT retry-safe
                                            → NOT a peer wire code
```

"The SDK reports NOTHING for such an exchange" is gone. A hang is not a security product,
and the durable proposition is the one above.

### Security consequence

> An application driving MCP through this SDK cannot be handed another exchange's answer, an
> answer from a signer this deployment does not trust, or a refusal that verified only unbound
> — and cannot be told that an operation did not run when the SDK does not know that.
>
> The last is the one a byte-level fixture cannot reach, and the timeout is where it bites: a
> response deadline is the condition under which this SDK knows LEAST about whether the call
> ran, so a synthesized `not_executed` or retry-safe verdict there is exactly what makes a
> caller repeat a tool call that already executed. Parity fixtures pin what this SDK EMITS;
> whether it invents an execution fact for a receipt that stated nothing is behaviour, and the
> two SDKs have diverged there before.

### Scope

Four paragraphs. The family boundary and the concurrency/throughput exclusion are unchanged.
Two are new or rewritten:

> THE RESPONSE DEADLINE IS ENFORCED, AND ITS ROLE HERE IS BOUNDED. The aggregate read bound
> is real: the body is taken one underlying read at a time and the socket's own timeout is
> narrowed to the time remaining, so a peer trickling under the per-recv bound cannot extend
> the read — measured against a real `http.client.HTTPResponse` fed by a real trickling
> socket, not against a fake with short-read semantics the production reader lacks. What this
> claim takes from that is not availability but honesty: the deadline fails the exchange as a
> LOCAL condition, and no execution or retry fact is manufactured for it.

> WHAT A CALLER DOES NEXT IS OUTSIDE THIS ROOT. This claim says nothing about an application's
> own retry policy, its outer timeouts, or what it concludes from a local failure; it says only
> that the SDK does not hand it a peer verdict or an execution fact the peer never gave. Retry
> policy outside this adapter is not this adapter's proposition.

The closing sentence dropped "beyond what ASM-0042 names": nothing about the transport below
is named any more, because nothing is assumed about it.

### Dependencies and owner

`depends_on = []`. `owner = "sdk_python.exchange_path"`, `review_requirement = "Owner
security-specification review"`, `supported_by = ["unit://sdk_python.exchange_path"]`. Root
family member beside THM-0095 and THM-0076.

---

## 2. The repair — what the aggregate deadline now is

`_read_bounded` in `sdk/python/python/mcp_re_sdk/mtls.py`. Two mechanisms, both required:

1. **`read1`, never `read`.** `HTTPResponse.read(n)` fills to `n`, so one call absorbs an
   unbounded number of underlying reads and the loop's deadline check is not reached while it
   does. `read1(n)` returns what ONE underlying read produced — chunked framing included — so
   control returns to the loop after every read the peer feeds.
2. **The socket's own timeout is narrowed to the time remaining.** `read1` still blocks inside
   a single recv; bounding that by `options.timeout` would let a peer that stops entirely
   overshoot by a whole per-recv bound. The socket is given the REMAINING time, so no recv can
   outlive the deadline, and the timeout it raises is reported as the aggregate bound because
   that is what it is. The connection is closed after the exchange, so the narrowed value is
   never inherited.

`max_response_bytes` is untouched and independent. `timeout=None` still means no bound on
either axis, and nothing narrows the socket when there is no deadline to narrow to.

`_READ_CHUNK_BYTES` was NOT reduced: a smaller fill size is a smaller window, not a bound.

---

## 3. Proof that the deadline works under production `http.client` semantics

The peer is a real socket. It accepts, consumes the request, sends a valid response head with
a 1 MB `content-length`, then writes one body byte every 10 ms for up to an 8 s budget. The
response is read through a real `http.client.HTTPResponse`. The per-recv (socket) bound is set
to **5 s** while the aggregate bound under test is **0.5 s**, so the socket is never idle long
enough to time out: what ends the read is the aggregate deadline or nothing.

| control | what it establishes |
|---|---|
| `test_the_aggregate_read_deadline_holds_against_a_real_trickling_peer` | the read ends at the deadline (0.4 s ≤ elapsed < 3.0 s), the peer had NOT finished its body, and it was trickling throughout — so the end came from the bound, not from the peer stopping |
| `test_the_full_mtls_path_bounds_a_trickling_peer` | the same over real TLS through the shipped `mtls_poster`, the only path that reaches the reader in production, with the single `timeout` a deployment actually sets |
| `test_a_response_inside_both_bounds_still_reads_whole` | the positive mirror on the same real peer: an ordinary reply is returned whole |
| `test_the_byte_ceiling_still_fails_closed_one_byte_past_it` | the ceiling holds with the clock nowhere near — the two bounds are independent |
| `test_no_timeout_means_no_aggregate_bound_either` | `timeout=None` bounds neither axis, and the socket's own timeout is left untouched |

**Both halves were shown load-bearing by removing them** (measured, then reverted):

| weakening | result |
|---|---|
| delete the deadline check and the socket narrowing, keep the per-recv timeout | `DID NOT RAISE MtlsTransportError` — both trickle controls RED |
| restore `read(want)` in place of `read1(want)`, keep the deadline check | `the aggregate deadline did not bound the read (8.01s)` — RED at the peer's budget |

The second is the exact defect R9-C010 recorded, and it is now caught by a control rather than
registered as a premise.

---

## 4. Proof that a timeout does not synthesize execution or retry facts

`test_a_transport_deadline_does_not_synthesize_what_the_peer_never_said`, in the root battery,
drives the pump with a poster raising precisely what `_read_bounded` raises, alongside a second
unrelated exchange. It asserts all five of the ruled points:

1. the affected exchange is failed and delivered, correlated to its own id;
2. the message carries the SDK's local prefix and **no** `mcp-re.*` peer verdict;
3. no `executionStatus` / `not_executed` anywhere in the rendered error;
4. no `retrySafety` / retry-safe verdict;
5. the unrelated exchange is unaffected and the correlation store is left empty.

**Measured negative control:** making `_one`'s local-failure path attach
`{"executionStatus": "not_executed", "retrySafety": "retry_safe"}` takes it RED
(`'not_executed' is contained here`). Reverted.

**The mutation lane cannot hold this as a probe.** `tools/verification/verify-mutations` runs
`cargo test -p <package>` and reads libtest output; it has no Python ecosystem. Registering a
`[[probe]]` naming a `.py` path and pytest node ids would be a probe no lane can execute —
the "configured but enforces nothing" shape this repository refuses. So the weakenings above
are recorded here as measurements, and extending the lane to the Python ecosystem is a
platform change this campaign did not make.

---

## 5. R9-C094 — old versus new, measured exactly

**The bogus control was NOT a declared `tested_symbol`.** Measured: the registered battery
named `test_transport.py`, `test_correlation.py`, `test_transport_replay.py`,
`test_malformed_elicitation.py` and `test_unrecognized_result_type.py` node ids only;
`test_the_response_read_is_bounded_in_wall_clock_not_only_per_recv` lived in
`sdk/python/tests/test_mtls.py` and appeared nowhere in `verification.toml`.

So the correction the ruling anticipated is the one that applies: **R9-C094 contaminated the
repository's apparent test coverage, not the declared 31-control theorem battery.** The root's
support closure never rested on it. What the closure DID rest on was the absence of any
deadline control at all — `mtls.py` is inside the unit's declared paths, so the property was
in the fingerprint's source closure with nothing measuring it. That gap is now closed from the
other side: the deadline controls above are registered, and the battery is 31 → 37.

The bogus control and its `_Trickle` fake are deleted, replaced by the production-shaped peer.
**The historical R9 rows are not edited.** `r9-dispositions.json` still reads
`"disposition": "SURVIVES_AND_MAPPED"`, `"theorem": null`, `"node": "no THM (SDK unrooted)"`
for both R9-C010 and R9-C094 — true when measured, and layer-1 raw evidence is never edited
into truth. The closure belongs in the next derived disposition.

---

## 6. ASM-0042 — discharged

Not renewed, and not replaced by a second assumption. The registry's existing convention is
followed exactly (ASM-0015, ASM-0022):

```
id                  ASM-0042            (retired, never reused)
description         WITHDRAWN — discharged by unit://sdk_python.exchange_path.
scope               []                  (no unit trusts it)
review_requirement  Withdrawn; nothing trusts it. Any future re-registration is a NEW id.
```

The justification records what it used to trust, why it was discharged rather than renewed —
the foreign `read(n)` fact is real and unchanged, but the obligation to enforce the deadline
MCP-RE advertises was local and repairable — and that the property is evidence now, not trust.

`verification/generated/assumption-consumers.md` renders it as `_no unit_ / _no theorem_`.
`check-assumptions` reports `PASS — every mechanism is registered`.

**Assumptions still consumed by `sdk_python.exchange_path`: none.**

---

## 7. Evidence and fingerprints

One unit, `unit://sdk_python.exchange_path`, class V0, evidence
`test://sdk_python/exchange_path/root_battery`, **37** declared controls, no assumptions.
Declared paths unchanged: `transport.py`, `correlation.py`, `authorization.py`, `custody.py`,
`mtls.py`.

The four propositions and their controls are as in the superseded packet, with the deadline
row added:

| proposition | controls |
|---|---|
| correlation | outstanding-entry teardown on failure and on close, one exchange's network error not taking down the session, the three `TestFailsClosed` cases |
| authenticity | recorded delegated session; one appended body byte; untrusted anchor; wrong epoch; wrong audience; revoked delegated key; unsigned acknowledgement; non-202 answer to a notification |
| terminality | terminal answer leg; unanswerable elicitation; declined elicitation; non-terminal reply without usable state; unrecognised result type |
| execution honesty | unbound rejection receipt; post-dispatch execution/retry contract; verified reply carrying a method; neither result nor error; verified rejection as error not result; wire failure as correlated JSON-RPC error; local signer failure and unexpected exception without a wire code; **and the transport-deadline control of §4** |
| the deadline itself | the five controls of §3 |

Two fixture negative controls (`test_the_fixture_is_otherwise_genuine_evidence`, in both the
malformed-elicitation and unrecognised-result-type files) remain, so a fixture that fails for
an unrelated reason cannot pass as evidence.

```
THM-0094  sha256:39511f98c6adac493545a6c12611aba3ed96ecc06057550bcf61836b6c40c489
  theorem_dependencies  {}
  review_requirement    Owner security-specification review

unit://sdk_python.exchange_path
          sha256:f32dbd440711958da59715fa0ffc0d5e60e806374402db2e176cce3a67c819e8
```

Both measured on the repair branch; the merged-main values are restated in the PR that lands
this packet, and the specification review must be recorded against the merged-main value.

Specification review: `UNREVIEWED`. Assumption axis: nothing left to review for this unit.
Battery: `pytest: 37 passed` under the lane's own invocation; the full Python suite is 218
passed, 1 skipped.

---

## 8. What is still outside

Concurrency and throughput. The transport below the adapter. The proxy's own guarantees. An
application that bypasses the adapter, and any application's retry policy. The TypeScript
member — THM-0095 carries its own premise (ASM-0043) and its own reader, and nothing here was
mirrored onto it.
